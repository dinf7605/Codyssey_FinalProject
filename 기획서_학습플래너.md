# 프로젝트 기획서 — 학습 목표 기반 맞춤 일정 플래너

> 서비스명(가제): **스터디페이스 (StudyPace)**
> 문서 버전: v1.0 · 작성일: 2026-09-11
> 기준 산출물: `기능명세서_학습플래너.xlsx`, 파이프라인 다이어그램(`-1.png`)

---

## 0. 한 줄 요약

> **"이 자격증, 지금 시작하면 기한 안에 끝낼 수 있나?"** 에 숫자로 답하고,
> 그 답을 **오늘 저녁 7시에 무엇을 공부할지**까지 자동으로 쪼개 주는 AI 학습 일정 플래너.

| 항목 | 내용 |
|---|---|
| 핵심 AI 기술 요소 | **AI Agent · RAG · 자동화 워크플로우 · Long-term Memory** (필수 2개 이상 → 4개 충족) |
| 기능 규모 | 총 69개 (Must 38 / Should 25 / Could 4 / Won't 2), AI 관련 21개 |
| 개발 기간 | 2026-09-14 ~ 2026-10-18 (5주) · 발표 2026-10-19~20 |
| 팀 구성 | 5인 · **전원 비전공자** · AI 도구를 활용해 개발 (7장) |
| 배포 형태 | 외부 공개 웹 서비스 (비회원도 핵심 가치 체험 가능) |
| 실사용자 테스트 | 5명 이상 · 2026-10-08 ~ 10-12 (5일간 실사용) |

---

## 1. 문제 정의

### 1-1. 배경

자격증·공모전 준비는 "무엇을 할지"보다 **"언제 무엇을 얼마나 할지"** 에서 무너진다.
시중 도구는 두 갈래로 나뉘는데, 어느 쪽도 이 간극을 메우지 못한다.

| 유형 | 예 | 한계 |
|---|---|---|
| 일정 관리 도구 | 구글 캘린더, Notion, Todoist | **사용자가 이미 계획을 갖고 있다고 전제**한다. "3개월 뒤 정보처리기사"를 입력해도 오늘 할 일로 쪼개주지 않는다 |
| 학습 콘텐츠 | 인강, 기출 사이트 | 커리큘럼은 주지만 **내 가용 시간에 맞춘 배치**를 해주지 않는다 |
| 정보 사이트 | 공모전 포털, 자격증 카페 | 정보는 많지만 **내 상황에 맞는지 판단**은 사용자 몫이다 |

즉 **"목표 → 오늘 할 일"** 사이의 변환을 아무도 대신해주지 않는다. 이 변환에는
(1) 학습량 추정, (2) 가용 시간 대조, (3) 선후 관계를 지킨 배치, (4) 밀렸을 때의 재배치
— 네 가지 계산이 매번 필요한데, 사람이 매일 손으로 하기엔 비용이 너무 크다.
그래서 대부분 **계획을 세우다 지치거나, 한 번 밀리면 계획 전체를 버린다.**

### 1-2. 해결할 문제 5가지

각 문제는 기능명세서의 구체적 기능 ID로 추적 가능하다.

| # | 문제 | 사용자의 실제 말 | 해결 기능 ID |
|---|---|---|---|
| **P1** | **목표가 없거나 막연하다** — 뭘 준비해야 할지 모른 채 "뭐라도 해야 하는데"에 머문다 | "취업 준비해야 하는데 뭘 따야 할지를 모르겠어요" | `FR-GOAL-01` `FR-GOAL-03` `FR-GOAL-05` `FR-GOAL-11` |
| **P2** | **기한 안에 되는지 모른다** — 근거 없이 목표를 잡았다가 중도 포기한다 | "2개월이면 될 줄 알았는데 한 달 해보니 반도 못 왔어요" | `FR-GOAL-04` `FR-GOAL-09` `FR-GOAL-10` `FR-CONT-10` |
| **P3** | **계획을 실행 단위로 못 쪼갠다** — "매일 2시간"까지는 정하는데 오늘 뭘 볼지에서 막힌다 | "오늘 뭐부터 해야 되지, 하다가 30분 날려요" | `FR-PLAN-02` `FR-PLAN-03` `FR-MAIN-03` |
| **P4** | **한 번 밀리면 계획 전체가 무너진다** — 재계획 비용이 커서 아예 포기한다 | "이틀 밀렸더니 다시 짜기가 귀찮아서 그냥 접었어요" | `FR-PLAN-06` `FR-PLAN-07` `FR-PACE-02` `FR-PACE-03` |
| **P5** | **지금 잘 가고 있는지 모른다** — 피드백이 없어 동기가 떨어지고 기회 정보도 놓친다 | "이 속도가 맞는 건지 모르겠어요" / "그 공모전 어제 마감이었대요" | `FR-PACE-01` `FR-PACE-04` `FR-CONT-04` `FR-UI-01` `FR-STUDY-04` |

### 1-3. 우리가 만드는 차이

> 기존 도구는 **사용자가 계획을 갖고 오면 관리**해 준다.
> 우리는 **목표만 갖고 와도 계획을 만들고, 밀리면 대신 다시 짠다.**

이 차이는 P3·P4에서 갈리며, 두 문제 모두 **매일 반복되는 계산**이라 자동화 가치가 가장 큰 지점이다.

---

## 2. 타겟 사용자

### 2-1. 사용자 정의

| 구분 | 대상 | 근거 |
|---|---|---|
| **주 타겟** | 자격증·공모전을 준비하는 대학생 / 취업 준비생 (20~26세) | 목표는 있으나 학기·알바로 **가용 시간이 불규칙**해 고정 플래너가 맞지 않음. 캘린더 연동 효용이 가장 큼 |
| **부 타겟** | 직무 전환·재취업 준비 직장인 (27~35세) | 주중 가용 시간이 적어 **기간 실현 가능성 판정(P2)** 의 가치가 특히 큼 |
| **제외** | 초·중·고 학생 | 학교 시간표가 고정되어 캘린더 기반 빈 슬롯 추출의 효용이 낮고, 미성년자 개인정보 처리에 보호자 동의 절차가 별도로 필요해 5주 범위를 벗어남 |

### 2-2. 페르소나

**① 김지훈 · 24세 · 컴퓨터공학과 3학년 — "목표는 있는데 계획이 없는 유형"**

- 상황: 3개월 뒤 정보처리기사 필기. 주 15시간 정도 낼 수 있지만 요일마다 다름
- 실패 경험: 엑셀로 계획표를 만들었으나 2주 만에 밀려서 폐기
- 기대: "내 시간표 보고 알아서 짜 줬으면. 밀려도 알아서 당겨줬으면"
- 관련 기능: `FR-PLAN-01`(캘린더 연동) → `FR-PLAN-02`(학습 분해) → `FR-PLAN-03`(배치) → `FR-PLAN-06`(야간 재조정)

**② 박서연 · 22세 · 시각디자인과 2학년 — "목표 자체가 없는 유형"**

- 상황: 포트폴리오는 만들고 싶은데 어떤 공모전이 자기 수준·일정에 맞는지 모름
- 실패 경험: 공모전 사이트를 둘러보다 마감 임박한 것만 발견하고 포기
- 기대: "내 관심 분야랑 낼 수 있는 시간 알려주면 할 만한 걸 골라줬으면"
- 관련 기능: `FR-GOAL-01`(관심분야) → `FR-GOAL-03`(후보 매칭) → `FR-GOAL-04`(기간 적합성) → `FR-GOAL-05`(추천 카드)

### 2-3. 사용자 여정 — 비회원에게 가치를 먼저 보여준다

```
[비회원]  공모전 검색(FR-CONT-03) → 준비 기간 계산(FR-CONT-10) → 목표 탐색·추천(FR-GOAL-01~05)
                                                                       │
                              ─── 가치를 체험한 뒤 ────────────────────┤ 여기서 가입 전환
                                                                       ▼
[회원]    가입(FR-JOIN-01~03) → 탐색 결과 인계(FR-GOAL-13 / FR-CONT-11) → 목표 확정(FR-GOAL-07)
                             → 캘린더 연동(FR-PLAN-01) → 일정 생성 → 매일 학습 · 자동 재조정
```

**설계 의도:** 가입을 먼저 요구하면 P1·P2 사용자는 가치를 보기 전에 이탈한다.
그래서 **AI 추천까지는 비회원에게 열고, 저장이 필요한 시점(목표 확정)에서만 가입을 요구**한다.
대신 비용·봇 대응으로 비회원 AI 호출을 24시간 3회로 제한한다(`FR-GOAL-12`).

---

## 3. 서비스 구조 — 4개 파이프라인

첨부한 파이프라인 다이어그램의 4개 흐름이 서비스의 전부다.
각 파이프라인은 **실행 주기가 다르고**, 그래서 **구현 방식도 다르다.**

| # | 파이프라인 | 실행 시점 | 단계 | 핵심 기술 |
|---|---|---|---|---|
| **0** | 목표 탐색 | 목표 없는 신규 사용자만, 1회 | 관심분야 입력 → 후보 매칭 → 기간 적합성 → 목표 추천 3~5장 | **RAG** + 결정론적 계산 |
| **1** | 일정 생성 | 목표 확정 시 1회 + 매일 03:00 재조정 | 목표 확정 → 캘린더 연동 → 학습 분해 → 스케줄 배치 | **AI Agent** + 규칙 엔진 |
| **2** | 공모전 추천 | 수집 매일 05:00, 추천 주 1회 월 09:00 | 공고 수집 → 임베딩 색인 → 검색·필터 → 재랭킹 | **RAG** + **자동화** |
| **3** | UI 적응 | 학습 이벤트 발생 시 실시간 | 학습 이벤트 → 실시간 집계 → 상태 판정 → 테마·밀도 적응 | **자동화** + **Memory** |

### 기능 범위 (총 69개)

| 우선순위 | 건수 | 범위 결정 |
|---|---|---|
| Must | 38 | 5주 내 100% 구현 — 없으면 서비스가 성립하지 않음 |
| Should | 25 | 4주차까지 목표, 일정 압박 시 가장 먼저 조정 |
| Could | 4 | 여유 시에만 (`FR-CONT-06` 포스터 정보 추출 등) |
| Won't | 2 | **명시적 제외** — `FR-STUDY-06`(필기 사진 요약), `FR-CONT-09`(팀원 매칭) |

> **Won't을 남겨두는 이유:** 멀티모달 기능 2건을 5주 안에 넣으면 품질이 나오지 않는다.
> 지우지 않고 "검토했고 의도적으로 뺐다"를 문서에 남긴다.

---

## 4. AI 활용 방식

### 4-1. 필수 기술 요소 충족 현황

| 기술 요소 | 충족 | 적용 기능 | 비고 |
|---|:---:|---|---|
| **AI Agent** | O | `FR-PLAN-02` 학습 분해 에이전트 | 도구 6개 · 최대 반복 5회 |
| **RAG** | O | `FR-GOAL-03/05/11`, `FR-CONT-02/04/05`, `FR-MAIN-05` | 2개 컬렉션 분리 색인 |
| **자동화 워크플로우** | O | `FR-CONT-01`, `FR-PLAN-06`, `FR-ALARM-01~04`, `FR-STUDY-03`, `FR-MY-03` | 배치 4종 + 이벤트 집계 |
| **Long-term Memory** | O | `FR-MEM-01/02`, `FR-GOAL-08`, `FR-CONT-08` | 감쇠 규칙 + 사용자 삭제권 |
| 멀티모달 | △ | `FR-CONT-06`(Could), `FR-STUDY-06`(Won't) | **범위 외로 명시** |

> 요구사항은 2개 이상이며, 본 프로젝트는 **4개**를 핵심 경로에 배치한다.

### 4-2. 핵심 설계 원칙 — LLM을 쓰는 곳과 쓰지 않는 곳을 먼저 가른다

이 프로젝트에서 **가장 중요한 기술적 의사결정**이다.

| 구간 | 방식 | 해당 기능 | 그렇게 정한 이유 |
|---|---|---|---|
| **판정·계산** | **LLM 미사용 (결정론적)** | `FR-GOAL-04` 기간 적합성 · `FR-GOAL-09` 최소 기간 · `FR-PLAN-03` 스케줄 배치 · `FR-PACE-02` 진도 기준선 | 같은 입력에 **항상 같은 결과**가 나와야 검증이 가능하다. "어제와 다른 답"이 나오는 일정표는 신뢰할 수 없고, 규칙 위반(마감 초과·선후 위반)을 **0건으로 보장**할 수 없다 |
| **분해·언어화** | **LLM 사용** | `FR-PLAN-02` 학습 분해 · `FR-GOAL-05` 추천 이유 · `FR-CONT-05` 추천 이유 · `FR-PLAN-07` 재조정 사유 | 목표를 학습 단위로 쪼개고 이유를 문장으로 만드는 일은 규칙으로 열거할 수 없다 |

**결과:** LLM이 실패해도 **일정은 항상 만들어진다.**
분해가 실패하면 표준 커리큘럼 템플릿으로 대체하고, 배치 로직은 그대로 동작한다.
이것이 폴백 정책(4-8)이 실제로 작동하는 구조적 근거다.

### 4-3. AI Agent — 학습 분해 에이전트 (`FR-PLAN-02`)

**보유 도구 6종**

| 도구명 | 목적 | 실패 시 동작 |
|---|---|---|
| `get_goal_catalog` | 관심분야에 맞는 자격증·공모전 후보 검색 | 빈 배열 → 관심분야 재입력 요청 |
| `get_available_slots` | 캘린더·수동 입력에서 빈 학습 슬롯 조회 | 캘린더 실패 시 수동 가용시간만 반환 |
| `search_curriculum` | 목표의 표준 커리큘럼·출제 범위 검색 | 빈 배열 → 일반 템플릿 분해 + '추정' 표시 |
| `estimate_effort` | 학습 단위별 예상 소요시간 추정 | 카탈로그 표준 시간의 단위 평균값 사용 |
| `search_contests` | 관심 태그·목표로 공모전 공고 검색 | 빈 배열 → '이번 주 추천 없음' |
| `save_plan` | 확정된 학습 블록을 일정에 저장 | 전체 롤백 후 재시도 버튼 (**사용자 확인 필요**) |

**실행 정책**

| 항목 | 설정값 | 근거 |
|---|---|---|
| 최대 도구 호출 반복 | **5회** | 3회로는 "커리큘럼 검색 → 소요시간 추정 → 배치" 연계가 끊겼고, 8회 이상은 결과 차이 없이 비용만 증가 |
| 반복 초과 시 | 그때까지 분해된 단위로 일정 생성 + '일부만 생성됨' 배지 | 아무것도 못 만들면 사용자가 다음 행동을 할 수 없다 |
| 도구 선택 불가 시 | 표준 커리큘럼 템플릿으로 분해 + '추정 기반' 명시 | 근거 없는 분해를 근거 있는 것처럼 보이게 하면 안 된다 |
| 사용자 확인 필요 도구 | `save_plan`, 구글 캘린더 쓰기(`FR-PLAN-08`) | 실제 캘린더에 이벤트가 생기는 **되돌리기 어려운 동작** |
| 배치 실행 상한 | 야간 재조정은 사용자당 도구 호출 **3회** | 무인 실행이라 상한이 없으면 비용이 그대로 폭증 |
| 호출 로깅 | 도구명·파라미터·소요시간·토큰 수·성공 여부 전량 기록 | 실패 추적 + `FR-ADMIN-02` 대시보드의 근거 |

> **최대 반복 횟수를 먼저 정한 이유:** 상한이 없는 에이전트 루프는 API 비용 사고의 1순위 원인이다.
> 특히 야간 재조정은 **무인 실행**이라 사람이 중단시킬 수 없어 별도로 더 낮은 상한(3회)을 둔다.

### 4-4. RAG — 2개 컬렉션 분리 색인

| 단계 | 설정값 | 선정 근거 |
|---|---|---|
| 대상 컬렉션 | ① 목표 카탈로그(자격증·공모전 요강) ② 공모전 공고 | 스키마와 갱신 주기가 달라 한 인덱스에 섞으면 필터 조건이 충돌 |
| 문서 로더 | 공개 API(JSON) 우선, 미제공 출처만 HTML 파서 | 공고 대부분이 API로 제공되어 크롤링은 보조 수단 |
| 청킹 / 오버랩 | **600자 / 80자** | 공고 본문 평균 약 900자. 400자는 '지원 자격'이 잘리고 1200자는 무관 항목이 섞임 (샘플 30건 비교) |
| 임베딩 모델 | `text-embedding-3-small` (1536차원) | 상위 모델과 한국어 공고 검색 정확도 차이가 유의미하지 않으면서 비용 약 1/6 |
| 벡터 스토어 | PostgreSQL + **pgvector (HNSW)** | 공고 1만 건 이하 규모에서 전용 벡터DB의 이점이 없고 운영 대상만 늘어남 |
| 검색 방식 | 벡터 검색 + 메타 필터(마감일 ≥ 오늘, 자격 요건 충족) | 필터 없이 검색하면 **이미 마감된 공고가 상위**에 올라오는 문제를 1차 테스트에서 확인 |
| top-k | 후보 20건 → 재랭킹 후 **상위 5건** | 20건 이후 순위에서 재랭킹 상위 5에 진입한 사례가 없었음 |
| 유사도 임계값 | 코사인 **0.62**(공모전) / **0.60**(목표 후보) | 0.5는 무관 분야가 통과하고 0.7은 정상 질의도 차단. 정답셋 기준 균형점 |
| 재랭킹 | 유사도 0.5 + 마감 임박도 0.3 + 자격 적합도 0.2 가중합 | 유사도만 쓰면 **준비 기간이 부족한 공고**가 상위에 남아 실제로 지원할 수 없음 |
| 검색 쿼리 소스 | ① 입력 관심 태그 ② 진행 중·최근 목표 태그(회원) ③ 세션 내 조회 공고 분야(비회원) | 관심분야 입력을 필수에서 뺐기 때문에 쿼리가 비는 경우가 생김. 셋 다 없으면 검색 대신 **인기 목표 목록**으로 대체 (콜드스타트 경로) |

### 4-5. 자동화 워크플로우

| 워크플로우 | 주기 | 기능 ID | 실패 시 |
|---|---|---|---|
| 공고 수집 | 매일 05:00 | `FR-CONT-01` | 해당 출처만 건너뜀 · 3회 연속 실패 시 관리자 알림 |
| 임베딩 색인 | 수집 직후 | `FR-CONT-02` | 2회 재시도 후 미색인 보류 → 다음 배치 재처리 |
| 야간 일정 재조정 | 매일 03:00 | `FR-PLAN-06` | **기존 일정 유지** + 다음 날 재시도 |
| 주간 공모전 추천 | 매주 월 09:00 | `FR-CONT-04` | 유사도 미달 시 '이번 주 추천 없음' |
| 학습 알림 | 블록 10분 전 / 종료 30분 후 / 21시 | `FR-ALARM-01~03` | 1회 재시도 → 다음 알림에 합침 · 3회 실패 시 채널 전환 |
| 주간 요약 메일 | 매주 일 20:00 | `FR-ALARM-04` | 수신 거부 시 발송 제외 |
| 실시간 집계 | 이벤트 발생 시 | `FR-STUDY-03` | 큐 보관 후 복구 시 재집계 |

> **야간 재조정의 최우선 원칙은 "실패해도 기존 일정이 깨지지 않는 것"** 이다.
> 무인 실행이라 사용자가 아침에 빈 일정표를 보는 상황이 가장 나쁘다.

### 4-6. Long-term Memory

| 항목 | 내용 |
|---|---|
| 저장 대상 | 선호 학습 시간대 / 학습 단위별 **예상-실제 소요시간 편차** / 관심 태그 / 추천 거절 이력 / 최근 4주 완료율 |
| 저장 시점 | 블록 완료 직후(편차), 추천 평가 직후(거절 이력), 야간 배치 시점(완료율) |
| 저장소 | PostgreSQL `user_memories` — **벡터 미사용** (검색이 아니라 프롬프트 주입이 목적) |
| 갱신 규칙 | 소요시간 편차 = 최근 10회 **지수이동평균** / 거절 태그 가중치 = 4주 경과 시 **절반 감쇠** |
| 활용 방식 | 학습 분해의 예상시간에 **편차 계수를 곱하고**, 스케줄 배치 시 선호 시간대를 우선 배정 |
| 주입 상한 | 시스템 프롬프트 직후 `user_context` 블록, **최대 300토큰** (초과 시 최근 갱신 항목만 유지) |
| 보관 기간 | 최종 로그인 후 12개월, 이후 자동 삭제 (`NFR-PRIV-01`) |
| 사용자 삭제 | 마이페이지에서 항목별·전체 **즉시 물리 삭제** (`FR-MEM-02`) |

> **개인화 서비스에서 "쌓이기만 하는 메모리"는 설계 실패다.**
> 감쇠 규칙 · 토큰 상한 · 사용자 삭제권 세 가지를 함께 정의한 이유.

### 4-7. 프롬프트 설계

| 프롬프트 | 역할 | 주요 제약 | 출력 형식 |
|---|---|---|---|
| 공통 시스템 | 학습 코치 페르소나 | 모르면 모른다고 답할 것 / **캘린더 일정의 제목·참석자는 프롬프트에 넣지 않고 시간대만 사용** | — |
| 목표 추천 | 관심분야 기반 목표 큐레이터 | 검색된 후보 범위 밖의 목표를 만들지 말 것 / 기간 적합성 계산 결과를 뒤집지 말 것 / 이유 2문장 이내 | JSON(`goal_id`, `reason`) |
| 학습 분해 | 커리큘럼 설계자 | 단위 30~120분 / 선행 관계 명시 / 검색 범위 밖은 '추정' 표시 | **고정 JSON 스키마** |
| 추천 이유 | 공모전 큐레이터 | 공고에 적힌 사실만 사용 / **지원 자격 충족을 단정하지 말 것** | 평문 1문장 |
| 재조정 사유 요약 | 일정 도우미 | 100자 이내 / **사용자를 책망하는 표현 금지** / 다음 행동 1가지만 제안 | 평문 |

> 프롬프트 전문은 저장소의 `prompts/` 디렉터리에 파일로 관리하고 버전 태그를 붙인다.
> 측정 이력(4-9)과 프롬프트 버전을 연결해야 "무엇을 바꿔서 올랐는지"를 증명할 수 있다.

### 4-8. AI 실패·폴백 정책

LLM은 반드시 실패한다. 실패 시 동작을 **기능이 아니라 정책으로** 미리 정의한다.

| 실패 상황 | 감지 방법 | 시스템 동작 | 사용자에게 보이는 메시지 |
|---|---|---|---|
| LLM 타임아웃 | 20초 내 응답 없음 | 표준 커리큘럼 템플릿으로 대체 + 로그 | "시간이 걸려 기본 계획으로 시작합니다" |
| JSON 스키마 검증 실패 | 스키마 검증 실패 | 동일 프롬프트 1회 재시도 → 템플릿 분해 | "자동 생성에 실패해 기본 계획으로 시작합니다" |
| 검색 결과 없음 | 최고 유사도 < 0.62 / 0.60 | **사전 지식으로 답하지 않고 추천 중단** | "조건에 맞는 공모전을 찾지 못했습니다" |
| 캘린더 API 실패 | 401·5xx 또는 10초 초과 | 수동 가용시간만으로 일정 생성 + 재연동 배너 | "직접 입력한 시간으로 일정을 만들었습니다" |
| 배치 슬롯 부족 | 잔여 가용시간 < 필요 학습시간 | 초과분 미배치 + 기간 연장·범위 축소안 제시 | "남은 기간에 ○시간이 배치되지 않았습니다" |
| 토큰 한도 초과 | 요청 전 토큰 카운트 | 메모리 주입 축소 → 청크 5→3 → 그래도 초과 시 거부 | "입력이 너무 깁니다. 관심분야를 줄여 주세요" |
| 공고 수집 실패 | 출처 4xx·5xx 또는 파싱 오류 | 해당 출처만 건너뜀, 3회 연속 시 관리자 알림 | (사용자 노출 없음) |
| 야간 재조정 실패 | 배치 잡 예외 발생 | **기존 일정 그대로 유지** + 다음 날 재시도 | "기존 일정을 유지했습니다" |
| 일일 요금 한도 초과 | 누적 API 비용이 임계치 도달 | **신규 AI 호출만 차단, 비AI 기능은 정상 유지** | "오늘의 AI 이용 한도에 도달했습니다" |
| 알림 발송 실패 | 푸시·메일 API 오류 | 1회 재시도 → 다음 예약 알림에 합침, 3회 실패 시 채널 전환 | (사용자 노출 없음) |

> **가장 중요한 줄:** '검색 결과 없음'에서 **사전 지식으로 답하지 않는다.**
> RAG 서비스가 근거 없이 답하기 시작하면 RAG를 쓰는 의미가 사라진다.

### 4-9. AI 품질 평가 방법

정답셋 20케이스를 만들고 **주 1회 측정 → 개선 → 재측정**한다.

| 평가 항목 | 측정 방법 | 목표 수치 |
|---|---|---|
| 목표 추천 적합도 | 관심분야 20케이스, 팀원 2명이 카드별 적합/부적합 교차 채점 | 상위 3장 중 2장 이상 적합 **80%** |
| 일정 실현 가능성 | 규칙 검증기(마감 초과·선후 위반·하루 상한 위반) | **100%** (미배치 명시 포함) |
| 학습 분해 품질 | 표준 커리큘럼 대비 누락 단원 비율 | **10% 이하** |
| 공모전 추천 정확도 | 추천 5건 중 자격·마감 조건을 모두 만족하는 비율 (Precision@5) | **90% 이상** |
| 추천 이유 사실성 | 이유 문장이 공고 원문으로 검증되는 비율 | **95% 이상** |
| 일정 준수율 | 배정 블록 대비 완료 블록 비율 (실사용자 5명) | **60% 이상** |
| 재조정 안정성 | 야간 재조정으로 위치가 바뀐 블록 비율 | **30% 이하** |
| 중간 목표 적정성 | 체크포인트 기준일에 해당 범위를 실제로 끝낸 비율 (실사용자 5명) | **70% 이상** |

**측정 일정**

| 회차 | 날짜 | 주요 개선 조치 |
|---|---|---|
| 1차 | 2026-09-22 (화) | 정답셋 20케이스 구축 후 **기준선 수립** |
| 2차 | 2026-09-29 (화) | 청킹 크기 · 유사도 임계값 조정 |
| 3차 | 2026-10-06 (화) | 재랭킹 가중치 조정 |
| 4차 | 2026-10-13 (화) | 실사용자 5명 테스트 결과 반영 |

> **발표의 핵심 슬라이드는 "무엇을 바꿨더니 몇 %가 올랐다"** 이다.
> 그래서 1차 측정을 개발 2주차에 미리 끼워 넣어 **기준선부터 확보**한다.

---

## 5. 기술적 접근 방식

### 5-1. 시스템 아키텍처

```
┌──────────────────────────────────────────────────────────────────────┐
│  클라이언트 (Next.js / Vercel)                                       │
│  대시보드 · 온보딩 · 일정 · 공모전 · 마이페이지 · 관리자             │
│  적응형 테마·밀도 (FR-UI-01/02)                                      │
└───────────────────────────────┬──────────────────────────────────────┘
                                │ HTTPS / JSON
┌───────────────────────────────▼──────────────────────────────────────┐
│  API 서버 (FastAPI / Railway)                                        │
│  ┌────────────┬─────────────┬────────────┬─────────────────────────┐ │
│  │ 인증·계정  │ 목표·온보딩 │ 일정·진도  │ 공모전 · 알림 · 관리자  │ │
│  └────────────┴─────────────┴────────────┴─────────────────────────┘ │
│  ┌───────────────────────────┬────────────────────────────────────┐  │
│  │  AI 레이어                │  결정론 레이어 (LLM 미사용)        │  │
│  │  · 학습 분해 Agent        │  · 기간 적합성 판정                │  │
│  │  · RAG 검색·재랭킹        │  · 스케줄 배치 엔진                │  │
│  │  · 이유 생성              │  · 진도 기준선 계산                │  │
│  │  · 폴백 / 스키마 검증     │  · 규칙 검증기                     │  │
│  └───────────────────────────┴────────────────────────────────────┘  │
└──────┬──────────────┬──────────────┬─────────────────┬───────────────┘
       │              │              │                 │
┌──────▼───────┐ ┌────▼────────┐ ┌───▼────────┐ ┌──────▼───────────┐
│ PostgreSQL   │ │   Redis     │ │ Anthropic  │ │ Google Calendar  │
│ + pgvector   │ │ (Upstash)   │ │ Claude API │ │ OAuth (읽기)     │
│ 업무 데이터  │ │ 실시간 카운터│ │ + 임베딩   │ │                  │
│ 벡터 색인    │ │ 스트릭·레벨 │ │   API      │ │                  │
│ user_memories│ │             │ │            │ │                  │
└──────▲───────┘ └─────────────┘ └────────────┘ └──────────────────┘
       │  배치 트리거 · 재시도 · 실패 알림
┌──────┴───────────────────────────────────────────────────────────────┐
│  Make (자동화 워크플로우)                                            │
│  05:00 공고 수집 │ 03:00 야간 재조정 │ 월 09:00 추천 │ 일 20:00 요약 │
└──────────────────────────────────────────────────────────────────────┘
```

> **1차 구현 조정 (팀 보유 기술 반영)** — 자동화는 팀이 이미 쓰는 **Make**로, 인증은 **Supabase Auth**로,
> 실시간 집계는 **PostgreSQL**로 시작한다. 위 그림의 Redis는 목표 구조이며 집계가 느려지는 것이 관측될 때 도입한다.
> 변경 근거와 역할별 학습 계획은 [학습 로드맵](docs/학습로드맵.md) 1장 참고.

### 5-2. 기술 스택과 선정 이유

| 영역 | 선택 | 선정 이유 |
|---|---|---|
| **백엔드** | Python 3.12 + **FastAPI** | ① 과제 목표가 "노코드 도구와 **Python** 조합"을 명시 ② LLM·임베딩 SDK 생태계가 Python 중심 ③ **Pydantic 스키마 검증**이 `FR-PLAN-02`의 "고정 JSON 스키마 반환" 요구와 그대로 맞물려, 스키마 검증 실패 시 재시도 정책(4-8)을 프레임워크 수준에서 구현 가능 ④ async로 LLM·캘린더·검색 대기를 동시 처리 |
| **LLM (주)** | **Claude Opus 5** (`claude-opus-5`) | 학습 분해는 **도구 호출 + 고정 JSON 스키마 준수**가 동시에 필요한 가장 어려운 구간. 도구 정의에 `strict: true`를 걸면 인자가 스키마를 반드시 통과하므로 "JSON 스키마 검증 실패" 폴백의 **발생 빈도 자체를 낮춘다**. 1M 컨텍스트 / 입력 $5 · 출력 $25 (per 1M tokens) |
| **LLM (보조)** | **Claude Haiku 4.5** (`claude-haiku-4-5`) | 추천 이유 1문장(`FR-CONT-05`), 재조정 사유 100자 요약(`FR-PLAN-07`)은 **짧고 호출 수가 많은** 구간이라 주 모델을 쓰면 비용이 호출 수에 비례해 늘어난다. 입력 $1 · 출력 $5로 분리해 `FR-ADMIN-02` 일일 비용 임계치를 관리 |
| **임베딩** | `text-embedding-3-small` (OpenAI) | 기능명세서에서 확정된 값이며 1536차원이 pgvector 스키마에 고정됨. 한국어 공고 검색 정확도 대비 비용 우위. **생성(Anthropic)과 임베딩(OpenAI)의 제공자가 갈리므로 키·비용 관리를 `FR-ADMIN-02`에서 기능별로 분리 집계** |
| **프론트엔드** | **Next.js (App Router)** + JavaScript | ① 공모전 검색·상세는 **비회원 공개**라 초기 로딩·SEO가 유입에 직결 ② 적응형 테마·밀도가 서버에서 내려주는 레벨 값에 따라 결정되므로 서버 렌더가 자연스러움 ③ Vercel 연동으로 **GitHub push → 자동 배포**가 무설정 — **팀이 이미 Vercel을 써 봤다** ④ TypeScript 대신 JavaScript: 전원 비전공자 팀에서 타입 에러는 막히는 시간을 크게 늘린다 |
| **DB · 인증** | **Supabase**<br>PostgreSQL + pgvector + **Auth** | pgvector·HNSW를 별도 인프라 구축 없이 즉시 사용. 업무 데이터·벡터·메모리를 **한 DB에서 조인**할 수 있어 "마감일 ≥ 오늘 + 자격 충족" 메타 필터를 SQL 한 번으로 처리. **Auth를 함께 쓰면 가입·로그인·비밀번호 재설정·해싱이 설정으로 해결**되어 `FR-AUTH-01~03` `FR-JOIN-01`이 구현에서 설정으로 바뀐다 |
| **실시간 집계** | **PostgreSQL** (1차)<br>→ Redis (확장 시) | `FR-STUDY-03`은 쓰기가 잦아 최종적으로는 Redis가 맞다. 다만 **실사용자 5~10명 규모에서는 `study_events` 테이블 집계로 충분**하고, 기술을 하나 덜 배우는 편이 5주 일정에 유리하다. 도입 기준(집계 지연 관측)을 미리 정해 둔다 |
| **자동화** | **Make** | **팀이 이미 쓸 줄 아는 도구**다. 배치 7종의 실행 이력·재시도·실패 알림이 UI로 보여 "3회 연속 실패 시 관리자 알림"(`FR-CONT-01`) 같은 운영 요건을 확인할 수 있다. **파싱·임베딩처럼 정확성이 중요한 로직은 Python 엔드포인트에 두고 Make는 트리거만 담당** — 무료 요금제의 실행 횟수(operations) 절약과 Git 버전 관리를 동시에 얻는다 |
| **배포** | Vercel(프론트) · Railway(백엔드) · Supabase(DB) | 전부 GitHub 연동 자동 배포 + 무료/저가 티어. **1주차에 배포 파이프라인을 먼저 완성**해 마지막 주 배포 리스크를 제거 |
| **CI** | GitHub Actions | PR 시 lint·test 자동 실행. 과제 요건인 **버전 관리 기록**이 자연스럽게 남음 |
| **인증** | **Supabase Auth**<br>+ Google OAuth | 비밀번호 해싱·세션·재설정 메일을 직접 만들지 않는다. 캘린더 연동이 필수라 OAuth는 어차피 필요하되, **캘린더 권한은 로그인이 아니라 온보딩에서 별도 요청**해 첫 진입 장벽을 낮춤(`FR-AUTH-02`). 비밀번호 조합 규칙(`FR-JOIN-01`)과 잠금 정책(`NFR-SEC-01`)은 Supabase 기본값으로 충족되는지 확인 후 부족분만 직접 구현 |

### 5-3. 데이터 모델 개요

| 도메인 | 주요 테이블 | 핵심 컬럼 |
|---|---|---|
| 계정 | `users`, `consents` | 이메일, 비밀번호 해시, 닉네임, 동의 유형·일시 |
| 목표 | `goals`, `goal_catalog`, `goal_catalog_embeddings` | 목표명, 기한, 주당 목표시간, 표준 학습시간, 임베딩 |
| 일정 | `study_units`, `blocks`, `block_changes` | 학습 단위명, 예상 소요시간, 선행 단위, 배치 일시, 고정 여부 |
| 진도 | `checkpoints`, `progress_snapshots` | 기준 일자, 목표 누적 단위 수, 기대·실제 진도, 지연 일수 |
| 학습 | `study_sessions`, `study_events`, `notes` | 시작·종료 시각, 순수 학습시간, 예상 대비 편차 |
| 공모전 | `contests`, `contest_embeddings`, `contest_feedback` | 제목·주최·분야·자격·마감일·원문 URL, 임베딩, 평가·당시 점수 |
| 개인화 | `user_memories` | 메모리 유형, 값, 근거, 갱신 일시 |
| 운영 | `ai_call_logs`, `notification_logs` | 기능 구분, 토큰 수, 소요시간, 비용, 성공 여부 |

> `contest_feedback`에 **당시 재랭킹 점수를 함께 저장**한다(`FR-CONT-08`).
> 이래야 추천이 틀렸을 때 **검색 문제인지 재랭킹 문제인지** 구분할 수 있다.

### 5-4. 비기능 요구사항 (NFR)

기능명세서가 `NFR-*` ID를 참조하고 있으나 정의 시트가 없어, 본 기획서에서 정의하고 명세서에 NFR 시트로 추가한다.

| ID | 항목 | 요구 내용 |
|---|---|---|
| `NFR-SEC-01` | 인증 보호 | 로그인 5회 연속 실패 시 60초 잠금 · 실패 사유 문구 통일로 **계정 존재 여부 비노출** · 로그인 화면에 비밀번호 조합 규칙 미노출 |
| `NFR-SEC-02` | 전송·저장 보안 | 전 구간 HTTPS · OAuth 리프레시 토큰은 애플리케이션 레벨 암호화 저장 · 비밀키는 환경변수로만 주입하고 저장소에 커밋 금지 |
| `NFR-SEC-03` | 비밀번호 정책 | bcrypt 해시 · 8~64자 영문·숫자·특수문자 각 1자 이상 · 이메일·닉네임 포함 및 상위 1만 건 빈출 비밀번호 거부 |
| `NFR-SEC-04` | 남용 방지 | 비회원 AI 호출 24시간 3회 제한 · IP는 해시로만 보관하고 24시간 후 파기 · 자동화 의심 시 캡차 |
| `NFR-PRIV-01` | 수집 최소화·고지 | 캘린더는 **시간대만 사용하고 제목·참석자는 저장·전송하지 않음** · 필수/선택 동의 분리 · 동의 일시 기록 · 최종 로그인 후 12개월 보관 |
| `NFR-PRIV-02` | 삭제권 | 메모리 항목별·전체 즉시 물리 삭제 · 탈퇴 시 일정·학습기록·메모리·캘린더 토큰 전체 삭제(최대 24시간) · 토큰은 즉시 폐기 |
| `NFR-ETHIC-01` | AI 생성 고지 | AI가 생성한 목표·일정·추천 이유에 **'AI 추천' 배지와 부정확 가능성 문구 상시 노출** · 접거나 숨길 수 없음 · 비회원 화면에도 동일 적용 |
| `NFR-ETHIC-02` | 사실성 | 검색된 공고 원문에 없는 내용 생성 금지 · **지원 자격 충족을 단정하지 않고 원문 확인 안내 병기** |
| `NFR-PERF-01` | 응답 성능 | 일반 조회 p95 1초 이내 · AI 생성 20초 타임아웃 · RAG 검색 3초 타임아웃 후 태그 완전일치 검색으로 대체 |
| `NFR-AVAIL-01` | 부분 장애 허용 | AI·캘린더·알림 중 하나가 실패해도 **나머지 기능은 정상 동작** · 집계 실패 시 직전 값 유지 |
| `NFR-COST-01` | 비용 상한 | 일일 API 비용 임계치 도달 시 신규 AI 호출 차단(비AI 기능 유지) · 비회원 추천을 먼저 차단 · 기능별 비용 분리 집계 |
| `NFR-A11Y-01` | 접근성 | 적응형 테마가 바뀌어도 **명도 대비는 레벨과 무관하게 WCAG AA 유지** · 색만으로 상태를 구분하지 않음(진도 신호등에 문구 병기) |

---

## 6. 일정 계획

**전체 기간: 2026-09-11 ~ 2026-10-20 (준비 3일 + 개발 5주 + 발표)**

### 6-1. 전체 일정

| 구간 | 기간 | 핵심 산출물 | 마일스톤 |
|---|---|---|---|
| 사전 준비 | 09-11(금) ~ 09-13(일) | 기획서 확정, 저장소·백로그, 초기 카탈로그 50건 | — |
| **1주차** | 09-14(월) ~ 09-20(일) | 인증·DB·**배포 파이프라인**, 공고 수집·색인, 공모전 검색 | **M1: 배포 URL에서 비회원 검색 동작** |
| **2주차** | 09-21(월) ~ 09-27(일) | 파이프라인 0·1 (목표 탐색 + 학습 분해 Agent + 배치) | **M2: 목표→일정 end-to-end** · 09-22 1차 측정 |
| **3주차** | 09-28(월) ~ 10-04(일) | 파이프라인 2 (RAG 추천), 학습 실행·집계, 야간 재조정 | **M3: 무인 배치 정상 동작** · 09-29 2차 측정 |
| **4주차** | 10-05(월) ~ 10-11(일) | 파이프라인 3, 메모리, 알림, 마이페이지, 관리자 | **M4: Must 38개 완료 · 실사용자 투입** · 10-06 3차 측정 |
| **5주차** | 10-12(월) ~ 10-18(일) | 피드백 반영, 안정화, 문서·발표 자료 | **M5: 개선 전후 비교표 완성** · 10-13 4차 측정 |
| 발표 | 10-19(월) ~ 10-20(화) | 발표 및 시연 | — |

### 6-2. 주차별 상세

**사전 준비 (09-11 ~ 09-13)**

- 기획서 확정 · 서비스명 결정 · 팀 역할 확정
- GitHub 저장소 생성: 브랜치 전략(`main` / `develop` / `feat/*`), PR·이슈 템플릿, `.env.example`
- 기능명세서 69행을 GitHub 이슈로 등록, NFR 시트 추가
- **목표 카탈로그 초기 데이터 50건 수동 구축** (자격증 30 + 공모전 20) — 콜드스타트 대응

**1주차 (09-14 ~ 09-20) — 기반 + 수집 파이프라인**

- 인증·가입: `FR-AUTH-01~03`, `FR-JOIN-01~03`
- DB 스키마 + pgvector 색인, Redis 연결
- **배포 파이프라인 완성 + 최초 배포** (프론트·백엔드·DB·Redis)
- 공고 수집 배치 `FR-CONT-01` · 임베딩 색인 `FR-CONT-02` (Make 시나리오)
- 공모전 검색·필터 `FR-CONT-03` · 준비 기간 산정 `FR-CONT-10`
- **DoD:** 배포 URL에서 **비회원이 공모전을 검색하고 준비 기간을 계산**할 수 있다
- *왜 1주차에 배포부터 하는가:* 배포를 마지막에 미루면 반드시 터진다. 또한 비회원 공개 화면이 먼저 있어야 **실사용자 모집 때 보여줄 것**이 생긴다

**2주차 (09-21 ~ 09-27) — 파이프라인 0·1 (AI Agent)**

- 목표 탐색 전체: `FR-GOAL-01~13`
- 캘린더 연동 `FR-PLAN-01` → 학습 분해 Agent `FR-PLAN-02` → 스케줄 배치 `FR-PLAN-03`
- 체크포인트 `FR-PACE-01` · 진도 기준선 `FR-PACE-02`
- **09-22(화) 1차 측정 — 정답셋 20케이스 구축, 기준선 수립**
- **DoD:** 목표 확정 → 일정 생성까지 end-to-end 동작, 규칙 검증기 위반 0건

**3주차 (09-28 ~ 10-04) — 파이프라인 2 (RAG) + 학습 실행**

- 유사 공모전 추천 `FR-CONT-04` · 추천 이유 `FR-CONT-05` · 재랭킹
- 학습 타이머 `FR-STUDY-01` · 완료 처리 `FR-STUDY-02` · 실시간 집계 `FR-STUDY-03`
- 야간 재조정 `FR-PLAN-06` · 변경 내역 `FR-PLAN-07` · 지연 감지 `FR-PACE-03`
- **09-29(화) 2차 측정 — 청킹 크기·유사도 임계값 조정**
- **DoD:** 주간 추천 배치와 야간 재조정이 **무인으로 3일 연속 성공**

**4주차 (10-05 ~ 10-11) — 파이프라인 3 + 메모리 + 실사용자 투입**

- 적응형 UI `FR-UI-01/02` · 레벨·스트릭 `FR-STUDY-04` · 진도 신호등 `FR-PACE-04`
- 메모리 `FR-MEM-01/02` · 알림 `FR-ALARM-01~04` · 마이페이지 `FR-MY-01~05`
- 관리자 `FR-ADMIN-01/02`
- **10-06(화) 3차 측정 — 재랭킹 가중치 조정**
- **10-08(목) ~ 10-12(월): 실사용자 5명 이상 실사용 테스트 (5일)**
- **DoD:** Must 38개 전부 동작, 테스트 사용자 온보딩 완료

**5주차 (10-12 ~ 10-18) — 피드백 반영 + 안정화**

- 10-12~13: 사후 인터뷰, 피드백 분류·우선순위 결정
- **10-13(화) 4차 측정 — 실사용자 결과 반영**
- 10-13~16: 개선 반영 및 회귀 테스트
- 10-15~17: README, 아키텍처 문서, **발표 자료** 작성
- **DoD:** 측정 1차 대비 4차 **개선 전후 비교표** 완성

### 6-3. 일정 리스크와 조정 규칙

5주에 Must 38 + Should 25 = 63개는 공격적인 목표다. **전원 비전공자 팀이므로 1~2주차의 학습 곡선까지 감안하면 더 그렇다.** 다음 규칙을 미리 정한다.

1. 주차별 DoD를 **금요일에 점검**한다
2. DoD 미달 시 **Should를 먼저 내린다** (Must는 범위에서 빼지 않는다)
3. Could 4건은 5주차에 여유가 있을 때만 착수한다
4. Won't 2건은 어떤 경우에도 복원하지 않는다
5. **실사용자 테스트 일정(10-08)은 고정**한다 — 밀리면 피드백 반영 시간이 사라진다
6. **범위 축소 트리거** — 3주차 시작(09-28) 시점에 **Must 진척이 40% 미만**이면 그 자리에서 Should 25건을 전부 `Won't`로 내리고 Must만 남긴다. "조금만 더 하면 된다"로 끌다가 4주차에 결정하면 이미 늦다
7. **막힌 기능은 사람을 바꾼다** — 한 기능이 3일 이상 진척 없으면 담당을 버디로 교체한다. 같은 사람이 계속 붙잡는 것이 가장 비싸다

---

## 7. 팀 구성 및 역할

**팀 전제: 5인 · 전원 비전공자 · AI 도구를 활용해 개발한다.**

이 전제가 역할 설계를 바꾼다. 기술 스택별로 나누면(프론트 담당 / 백엔드 담당) 서로의 코드를 이해하지 못해 통합 단계에서 멈추고, 한 명이 막히면 그 영역 전체가 멈춘다.
그래서 **기술이 아니라 "화면부터 API까지 하나의 기능 묶음"** 으로 나눈다.

### 7-1. 역할 분담 원칙

| # | 원칙 | 이유 |
|---|---|---|
| 1 | **수직 분할 — 각자 화면 + API를 끝까지 책임진다** | 비전공자에게 가장 어려운 것은 남이 만든 것과 붙이는 일이다. 한 사람이 한 기능을 끝까지 만들면 AI에게 "이 화면에서 이 API를 부르게 해줘"처럼 통째로 물을 수 있다 |
| 2 | **AI 기술 요소를 1인 1개씩 나눠 맡는다** | 과제 목표가 "본인의 역할과 기여를 설명할 수 있다"이다. 5명이 각각 다른 AI 기법을 맡으면 발표에서 5명 모두 설명할 거리가 생긴다 |
| 3 | **모든 영역에 버디(백업) 1명을 둔다** | 한 명이 아프거나 막혀도 진행된다. 지식이 1명에게만 있으면 그 사람이 곧 병목이다 |
| 4 | **1주차는 역할을 고정하지 않고 전원 공통 셋업** | 로컬 환경·DB 접속·배포를 전원이 한 번씩 직접 해본다. "환경은 ○○만 안다"가 되면 팀이 멈춘다 |
| 5 | **가장 어려운 2곳은 페어로 작업한다** | 학습 분해 Agent(`FR-PLAN-02`)와 스케줄 배치 알고리즘(`FR-PLAN-03`)은 난이도가 가장 높다. 혼자 맡기지 않는다 |

### 7-2. 역할 분담표

기능 69개를 **담당 묶음 단위**로 나눴다. 굵은 숫자는 담당 기능 수.

| 역할 | 담당 | 담당 기능 묶음 | 맡는 AI 기술 요소 | 버디 |
|---|---|---|---|---|
| **A · 프로젝트 리드** | (미정) | 대시보드 `FR-MAIN-01~06`, 진도 표시 `FR-PACE-01~04`, 적응형 UI `FR-UI-01/02` **(12)**<br>+ 문서·일정·사용자 테스트 운영·발표 | **AI 품질 평가** — 정답셋 20케이스, 4회 측정, AI 윤리 고지 점검 | E |
| **B · 목표 탐색** | (미정) | 온보딩 전체 `FR-GOAL-01~13`, 가용시간·목표 설정 `FR-MY-01/02` **(15)** | **RAG ①** — 목표 카탈로그 검색, 콜드스타트 대체 경로 | D |
| **C · 일정 · 학습** | (미정) | 일정 생성·재조정 `FR-PLAN-01~08`, 학습 실행·집계 `FR-STUDY-01~06` **(14)** | **AI Agent** — 학습 분해 에이전트, 도구 6종, 폴백 정책 | A |
| **D · 공모전 · 개인화** | (미정) | 공모전 전체 `FR-CONT-01~11`, 학습 메모리 `FR-MEM-01/02` **(13)** | **RAG ② + Memory** — 공고 색인·재랭킹, 메모리 감쇠 규칙 | B |
| **E · 기반 · 운영** | (미정) | 인증·가입 `FR-AUTH-01~03` `FR-JOIN-01~03`, 알림 `FR-ALARM-01~04`, 설정·탈퇴 `FR-MY-03~05`, 관리자 `FR-ADMIN-01/02` **(15)**<br>+ DB 스키마·배포·CI | **자동화 워크플로우** — n8n 배치 7종, 알림 파이프라인 | C |

**왜 이렇게 나눴는가**

- **A(리드)가 대시보드를 맡는 이유** — 대시보드는 B·C·D가 만든 결과를 모아 보여주는 화면이다. 리드가 맡으면 **통합이 안 되고 있다는 사실을 가장 먼저 발견**한다.
- **E가 인증·배포를 맡는 이유** — 다른 4명이 기능을 만들려면 로그인과 배포가 먼저 있어야 한다. **E의 1주차 산출물이 나머지 전원의 전제 조건**이다.
- **RAG를 B와 D가 나눠 맡는 이유** — 컬렉션이 2개(목표 카탈로그 / 공모전 공고)로 이미 분리되어 있어 자연스럽게 갈라진다. 같은 기법을 다른 데이터로 다루므로 서로 리뷰가 가능하다.

### 7-3. AI 협업 규칙 — 비전공자 팀의 핵심 장치

AI로 코드를 만드는 것은 빠르지만, **이해하지 못한 코드가 쌓이면 5주차에 고칠 수 없다.** 다음 6가지를 팀 규칙으로 못박는다.

| # | 규칙 | 왜 |
|---|---|---|
| 1 | **설명할 수 없는 코드는 병합하지 않는다** — PR 본문에 "이 코드가 무엇을 하는지" 3줄 설명 필수. 없으면 리뷰 반려 | 발표에서 "이 부분은 AI가 짜서 모릅니다"가 나오면 안 된다. 과제 평가 항목이기도 하다 |
| 2 | **쓴 프롬프트를 남긴다** — 기능 구현에 사용한 프롬프트를 PR 본문 또는 `docs/prompt-log/`에 기록 | 발표 자료의 "AI 활용 방식" 근거가 그대로 만들어진다 |
| 3 | **30분 룰** — 혼자 30분 막히면 반드시 팀 채널에 올린다 | 비전공자 팀에서 가장 큰 시간 손실은 혼자 오래 붙잡는 것이다 |
| 4 | **AI에게 물을 것과 팀에게 물을 것을 구분한다** — 문법·에러 메시지·라이브러리 사용법은 AI, "우리 서비스에서 이 동작이 맞는가"는 팀 | AI는 우리 기능명세서를 모른다. 사양 판단을 AI에 맡기면 명세와 다른 것이 만들어진다 |
| 5 | **실행해 보지 않은 코드는 커밋하지 않는다** | AI 생성 코드는 그럴듯하지만 안 돌아가는 경우가 흔하다 |
| 6 | **`.env` 값·API 키·개인정보를 프롬프트에 붙여넣지 않는다** | 외부 서비스로 비밀키가 전송된다. `NFR-SEC-02` 위반 |

### 7-4. 협업 절차

- 모든 작업은 **GitHub 이슈 → `feat/*` 브랜치 → PR → 리뷰 1인 이상 → `develop` 병합**
- **PR 리뷰는 버디가 아닌 사람이 한다** — 지식이 3명 이상에게 퍼지게 하기 위해서다
- 주 2회 스탠드업(월·목) — **"막힌 것"을 먼저 말한다.** 진척 보고보다 막힌 지점 공유가 우선이다
- 금요일 DoD 점검 (6-3절 조정 규칙 적용)
- 기여도는 **커밋·PR·리뷰·프롬프트 로그**로 증빙 — 과제의 "개인 기여 설명" 요건

### 7-5. 학습 곡선 대응

| 시점 | 조치 |
|---|---|
| 착수 전 (09-11~13) | 전원이 Git 기본 흐름(브랜치·PR·병합)을 **한 번씩 실습**한다. 더미 PR 1개씩 올려보고 시작 |
| 1주차 | 역할 고정 없이 **전원 공통 셋업** — 로컬 실행, DB 접속, 배포 확인을 각자 한 번씩 |
| 1주차 개인 과제 | 각자 **가장 쉬운 기능 1개**를 끝까지(화면 + API + PR) 해본다. 워크플로를 몸에 익히는 것이 목적 |
| 2주차부터 | 7-2 역할 고정. 어려운 구간(`FR-PLAN-02` `FR-PLAN-03`)은 **C + A 페어** |
| 상시 | 막힌 항목은 이슈에 `blocked` 라벨. 스탠드업에서 `blocked`부터 처리 |

---

## 8. 실사용자 테스트 계획

### 8-1. 개요

| 항목 | 내용 |
|---|---|
| 인원 | **5명 이상** (목표 7명 — 중도 이탈 대비) |
| 모집 | 자격증·공모전을 **실제로 준비 중인** 대학생·취준생. 학과 커뮤니티·단톡으로 모집 |
| 기간 | 2026-10-08(목) ~ 10-12(월), **5일간 실사용** |
| 사전 | 온보딩 지원 + 10분 사전 인터뷰(현재 준비 방식, 실패 경험) |
| 사후 | 15분 인터뷰 + 설문 |

### 8-2. 수집 데이터

**정량 — 서비스에 내장된 기능으로 수집**

| 지표 | 출처 | 목표 |
|---|---|---|
| 목표 추천 평가 | `FR-GOAL-08` 관심 있음/없음 + 사유 | 적합 80% |
| 공모전 추천 평가 | `FR-CONT-08` 도움됨/안 맞음 + 사유 + 당시 점수 | Precision@5 90% |
| 일정 준수율 | 배정 블록 대비 완료 블록 | 60% |
| 체크포인트 달성률 | 기준일에 범위 완료 여부 | 70% |
| 재조정 안정성 | 야간 재조정으로 이동한 블록 비율 | 30% 이하 |

**정성 — 인터뷰 질문**

1. 생성된 첫 일정을 보고 **바로 실행 가능하다고 느꼈는가**? 아니라면 무엇이 걸렸나
2. 일정이 밀렸을 때 **야간 재조정 결과를 신뢰**했는가
3. 알림 강도는 **도움이 되었는가, 부담이었는가** (`FR-ALARM-02` 독촉 강도 검증)
4. 추천 이유를 읽고 **왜 추천됐는지 납득**했는가
5. 이 서비스를 계속 쓸 의향이 있는가, 없다면 이유는

> 3번은 명세서에 "독촉 강도가 이탈로 이어지지 않는지 실사용자 테스트에서 확인 필요"로 남아 있던 **미확정 항목**이다. 테스트로 결론을 낸다.

### 8-3. 피드백 반영 절차

1. 수집 → 2. 유형 분류(버그 / UX / AI 품질 / 신규 요구) → 3. 영향도·빈도로 우선순위
→ 4. **GitHub 이슈 등록 (`user-feedback` 라벨)** → 5. 5주차 반영 → 6. 4차 측정으로 효과 확인

> 반영하지 않기로 한 피드백도 **이유와 함께 기록**한다.
> 발표에서 "무엇을 받았고 무엇을 왜 안 했는가"가 판단 근거가 된다.

### 8-4. 참여자 보호

- 개인정보 수집·이용 **동의서 서면 취득** (수집 항목·목적·보관 기간·파기 방법 명시)
- 테스트 종료 후 참여자가 원하면 **계정·데이터 전량 삭제** (`FR-MY-04`)
- 캘린더는 **시간대만 읽고 일정 제목·참석자는 저장하지 않음**을 사전 고지
- 인터뷰 녹음은 **별도 동의** 후 진행, 분석 완료 후 파기

---

## 9. AI 윤리 및 개인정보 보호

과제 필수 요건이며, **기능 행으로 명세서에 반영**되어 있다.

| 원칙 | 구현 | 기능 ID |
|---|---|---|
| **AI 생성 콘텐츠 명시** | 추천 카드·추천 이유에 'AI 추천' 배지 + 부정확 가능성 문구 **상시 노출, 숨김 불가** | `FR-GOAL-05` `FR-CONT-05` `NFR-ETHIC-01` |
| **가입 단계 고지** | "추천 목표와 학습 일정은 AI가 생성하며 부정확할 수 있습니다" 필수 동의. 문구 변경 시 재동의 | `FR-JOIN-03` |
| **비회원에게도 동일 적용** | 가입 전 화면이므로 동의 절차와 별개로 고지 문구를 상시 노출 | `FR-GOAL-05` |
| **단정하지 않기** | 지원 자격 충족을 단정하지 않고 **원문 확인 안내 병기** | `FR-CONT-05` `NFR-ETHIC-02` |
| **근거 없는 생성 금지** | 검색 실패 시 사전 지식으로 답하지 않고 '찾지 못함' 응답 | `FR-CONT-04` |
| **개인정보 최소 수집** | 캘린더는 **시간대만 사용**, 제목·참석자 미저장 | `FR-PLAN-01` `NFR-PRIV-01` |
| **동의 분리·기록** | 필수(이메일·학습이력·캘린더 시간대) / 선택(알림 수신) 분리, 동의 일시 DB 기록 | `FR-JOIN-02` |
| **삭제권 보장** | 메모리 항목별·전체 즉시 삭제, 탈퇴 시 토큰 즉시 폐기 | `FR-MEM-02` `FR-MY-04` `NFR-PRIV-02` |
| **압박하지 않는 표현** | 재조정 사유에 **사용자를 책망하는 표현 금지**, 알림 강도 3단계 + 방해금지 시간대 | `FR-PLAN-07` `FR-MY-05` |
| **수집 윤리** | robots.txt 금지 경로 제외, 출처별 이용약관 확인, 공개 API 우선 | `FR-CONT-01` |
| **접근성** | 테마가 바뀌어도 명도 대비 WCAG AA 유지, 색만으로 상태 구분하지 않음 | `NFR-A11Y-01` |

---

## 10. 리스크 관리

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | **공고 수집 출처의 약관·robots.txt 제약** | 파이프라인 2 전체 | 공개 API 우선, 금지 경로 제외, 출처별 약관 사전 확인. API 제공 출처만으로 최소 3개 확보 |
| R2 | **LLM 비용 폭증** | 서비스 중단 | 반복 5회·배치 3회 상한, 비회원 24h 3회, 일일 임계치 차단, 짧은 생성은 저가 모델 분리, 시스템 프롬프트·도구 정의 캐싱 |
| R3 | **카탈로그 데이터 부족 (콜드스타트)** | 추천 품질 | 사전 준비 기간에 **50건 수동 구축**, 검색 실패 시 인기 목록으로 대체하고 '추천 아님' 명시 |
| R4 | **Google OAuth 미검증 앱 제한** | 캘린더 연동 | 테스트 사용자 등록 방식으로 진행(100명 한도 내 충분). 연동 실패 시 수동 가용시간 경로가 이미 존재 |
| R5 | **5주 내 Must 38개 미완** | 과제 요건 | 주차별 DoD 금요일 점검, 미달 시 Should부터 하향, Won't 복원 금지 |
| R6 | **실사용자 5명 미확보 / 중도 이탈** | 과제 필수 요건 | **7명 모집**, 1주차에 공개 화면을 먼저 띄워 모집 시 실물 제시, 미완료자도 사후 인터뷰는 진행 |
| R7 | **야간 배치 실패로 아침에 빈 일정** | 신뢰도 치명 | 실패 시 **기존 일정 유지**가 기본 동작, 되돌리기 1회 제공, 관리자 로그·알림 |
| R8 | **개인정보 유출** | 법적·윤리 | 캘린더 제목 미저장, 토큰 암호화, IP 해시 24시간 파기, 비밀키 저장소 커밋 금지 |

---

## 11. 산출물 체크리스트

| 과제 요구 | 산출물 | 상태 |
|---|---|---|
| 기획서 | `기획서_학습플래너.md` (본 문서) | 작성 완료 |
| 기능 요구 명세서 | `기능명세서_학습플래너.xlsx` (69행 + AI기능명세) | 작성 완료 · **NFR 시트 추가 필요** |
| 시스템 아키텍처 | 본 문서 5-1 + 저장소 `docs/architecture.md` | 예정 |
| 기술 스택 선정 이유 | 본 문서 5-2 + [학습 로드맵](docs/학습로드맵.md) | 작성 완료 |
| AI 서비스 구현 | 생성형 AI·Agent 핵심 · 기술 요소 4개 | 예정 |
| 실사용자 테스트 | 5명 이상 · 테스트 리포트 | 예정 (10-08~12) |
| GitHub 저장소 | 소스·문서 · 브랜치·PR·이슈 기록 | 예정 (09-13까지 생성) |
| README | 개요, 팀원 역할, 기술 스택, 실행 방법, 결과 | 예정 |
| 발표 자료 | 문제 정의 / AI 활용·의사결정 근거 / 피드백·개선 | 예정 (10-17까지) |
| 서비스 배포 | 외부 접근 가능한 공개 URL | 예정 (**1주차 M1**) |

### 착수 전 확정해야 할 항목

1. **팀원 이름과 A~E 역할 매칭** (7-2절) — 역할 정의와 버디 짝은 확정, 이름만 채우면 된다
2. **서비스명 확정** — 현재 가제 "스터디페이스"
3. 기능명세서 미확정 항목 3건
   - 주당 최대 투입 시간을 가용시간의 80%로 볼지 100%로 볼지 (`FR-GOAL-09`)
   - 알림 강도 기본값을 '약'으로 할지 '보통'으로 할지 (`FR-MY-05`) → 실사용자 테스트로 결정
   - 레벨 하락 규칙을 넣을지 (`FR-STUDY-04`) → 이탈 우려로 현재는 미적용
4. **공고 수집 대상 출처 3곳 이상 확정** 및 약관 확인

---

*본 문서는 `기능명세서_학습플래너.xlsx`와 파이프라인 다이어그램을 근거로 작성했으며, 두 문서와 함께 버전 관리한다. 명세 변경 시 본 문서의 해당 절도 함께 갱신한다.*
